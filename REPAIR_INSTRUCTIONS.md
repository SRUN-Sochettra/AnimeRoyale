# AnimeRoyale Repair

Copy these files into the root of your `animeroyale` project, replacing existing files.

Then run:

```powershell
npm run lint
npm run build
```

If VS Code still shows old diagnostics, run `Developer: Reload Window`.
